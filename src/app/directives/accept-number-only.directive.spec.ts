import { AcceptNumberOnlyDirective } from './accept-number-only.directive';

describe('AcceptNumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AcceptNumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
